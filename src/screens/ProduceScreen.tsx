import React, {Component} from 'react';
import ContentContainer from '../components/ContentContainer';

export default class ProduceScreen extends Component {
  render() {
    return <ContentContainer title={'Termény'} />;
  }
}
