import React, {Component} from 'react';
import ContentContainer from '../components/ContentContainer';

export default class CurrencyScreen extends Component {
  render() {
    return <ContentContainer title={'Deviza'} />;
  }
}
