import React, {Component} from 'react';
import ContentContainer from '../components/ContentContainer';

export default class NewsScreen extends Component {
  render() {
    return <ContentContainer title={'Hírek'} />;
  }
}
