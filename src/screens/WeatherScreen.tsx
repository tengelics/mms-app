import React, {Component} from 'react';
import ContentContainer from '../components/ContentContainer';

export default class WeatherScreen extends Component {
  render() {
    return <ContentContainer title={'Időjárás'} />;
  }
}
